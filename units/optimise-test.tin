i32 test = 1;

fn i32 add(i32 x, i32 y)
{
    i32 result = x + y;
    return result;
}

fn i32 main
{
    test = test + 1;

    if (test == 2) {
        test = test + 1;
        test = 11;
    }

    print test;

    return 0;
}