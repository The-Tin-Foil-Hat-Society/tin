include "optimise_test_folder/opt_module_1.tin"
i32 test = 0;

fn void x
{
    test = 4;
}

fn void t
{
    t();
}

fn i32 main
{
    test = (test + 1) * 2;
    t();
    if (test < 1)
    {
        print "nope";
    }
    else if (test == 23)
    {
        print "still no";
    }
    else
    {
        print test;
    }

    return 0;
}