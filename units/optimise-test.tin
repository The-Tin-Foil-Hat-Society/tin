i32 test = 0;

fn i32 add(i32 x, i32 y)
{
    i32 result = x + y;
    return result;
}

fn i32 main
{
    i32 num1 = 2;
    bool t = true;

    i32 num2;
    num2 = 2;

    i32 num3;
    num3 = 1 + test;

    num2 = num2 * 3 + 1;
    num2 = num2 + num3;
    num2 = num2 + 1;

    if (num1 + 2 == 2) {
        num2 = 1;
    }
    else if (num3 * 3 == 3){
        num2 = 4;
    }
    else{
        print "test";
    }

    print num2;

    return 0;
}