i32 test = 0;

fn i32 add(i32 x, i32 y)
{
    i32 result = x + y;
    return result;
}

fn i32 main
{
    i32 num = 1;
    
    if (num == 1) {
        num = num + 1;
    }

    print num;

    return 0;
}