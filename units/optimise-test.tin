include "optimise_test_folder/opt_module_1.tin"
i32 test = 0;

fn i32 add(i32 x, i32 y)
{
    opt_module_1::test=0;
    return x;
}

fn i32 main
{
    test = test + 3;

    if (test == 3) {
        test = 0;
    }

    //opt_module_1::test = opt_module_1::test + 3;
    i32 b = 1;
    //i32 a = (opt_module_1::test * 3) + add(1,2);
    print b;
    print test;

    return 0;
}