i32 test = 0;

fn i32 add(i32 x, i32 y)
{
    i32 result = x + y;
    return result;
}

fn i32 main
{
    f32 float1 = 1.1;
    f32 float2 = float1 + 1.0;

    i32 num1 = 1;
    i32 num2;
    num2 = 2;

    i32 num3;
    num3 = 1 + test;

    num2 = num2 * 3 + 1;
    num2 = num2 + add(num2, num3);
    num2 = num2 + 1;

    bool t = (num2 + 1 >= 2 - 1);

    while (true == true) {
        if (true == true) {
            print "test";
        }
    }

    if (float1 > 1.0) {
        print "yep";
        if (true == false) {
            print "nope";
        }
        else {
            print "yep2";
        }
    }
    else {
        print "yep3";
    }

    if ((num2 + 1 >= 2 - 1)) {
        num2 = 1;
    }
    else if (num3 * 3 + 1 != 3 + 1){
        num2 = 4;
    }
    else if (true == false){
        print "pog";
    }
    else{
        print "test";
    }

    print num2;

    return 0;
}